# Auth (required in production; optional in development)
JWT_SECRET=
JWT_EXPIRES_IN=24h

# Optional custom path for page data override
LOCAL_DATA_PATH=

# Admin login credentials are read from src/data/admin.json
# Optional writable override: data/local/admin.json

# Site
NEXT_PUBLIC_SITE_URL=http://localhost:3000
NEXT_PUBLIC_WHATSAPP_NUMBER=+1234567890
